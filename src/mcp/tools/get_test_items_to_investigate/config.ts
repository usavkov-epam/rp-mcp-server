import { inputSchema, outputSchema } from "./schema";

export default {
  title: "Get recent test items marked for investigation",
  description: "Fetch recent test items marked for investigation from ReportPortal",
  inputSchema,
  outputSchema,
};
